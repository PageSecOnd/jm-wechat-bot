# Changelog

## 1.0.0

- 首个公开稳定版本；版本号从 0.x 提升为 1.0.0。
- 公开项目与 GitHub 仓库名称统一为 `jm-wechat-bot`；内部 Python 包、CLI 与 `JMWXBOT_` 配置前缀保持兼容。
- 整合 v0.6.2 的 JM 认证稳定性修复：登录成功后将完整 Cookie 与实际认证 API 域名绑定，认证请求不再携带同一 Cookie 跨域漂移。
- 整合 v0.6.1 的收藏夹分页与幂等收藏修复。
- 保留多微信账号/多联系人隔离、下载队列、每日签到、缓存管理和只读 Web 管理控制台。
- GitHub 发行包补齐 MIT License、第三方开源声明、GitHub Actions、贡献与安全说明。

## 0.6.2

- 修复 JM 登录态与 API 域名不绑定导致的间歇性 401：登录成功后固定使用实际接受登录的 API 域名。
- `/login` 使用隔离的一次性会话按最多 3 个当前 API 节点尝试登录；每次节点尝试都使用独立 Cookie，不跨节点复用认证状态。
- 不再只保存 AVS；现在保存上游登录后产生的完整 Cookie 集合以及认证域名，密码仍不会落盘。
- `/fav`、`/fav-add`、`/daily` 等认证接口不再携带同一登录 Cookie 自动跨域重试。认证域名失效时明确要求重新 `/login`。
- v0.6.1 及更早生成的 AVS-only profile 因缺少认证域名，升级后需重新 `/login` 一次。
- `/profile` 会识别旧认证 profile，避免把不可安全复用的旧登录态显示为“已验证”。

## 0.6.1

- 修复 `/fav` 将 JM API 的 20 条分页与机器人 8 条展示上限混用，导致“共 18 项但仅显示 8 项且 1/1 页”。
- `/fav` 现在按机器人自己的每页 8 条正确分页；18 项会显示为 3 页（8 + 8 + 2）。
- 修复 `/fav-add` 依赖上游 `status` 字段导致 `KeyError: 'status'`。
- `/fav-add` 改为幂等“确保已收藏”：收藏前先检查，缺失时调用原始 toggle 接口，再复查收藏夹确认写入。
- 已收藏项目再次执行 `/fav-add` 不会被 toggle 成取消收藏。

## 0.6.0

- 新增只读 Web 管理控制台。
- 显示绑定微信、联系人使用统计、JM 登录/签到状态、任务、错误、队列、缓存和磁盘数据。
- 新增隐私最小化命令事件统计，仅记录命令类型。
- 控制台默认关闭；启用时要求管理员 token，Compose 默认只映射到 127.0.0.1:8787。
- 延续 v0.5.3 的 JM 登录 Cookie 隔离与跨账号返回校验。
- 延续 v0.5.2 的封面标准化与坏缓存自修复。
